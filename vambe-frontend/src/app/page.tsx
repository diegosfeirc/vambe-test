import Hero from '@/components/Landing/Hero';

export default function Home() {
  return (
    <main style={{ margin: 0, padding: 0, minHeight: 'calc(100vh - 4rem)' }}>
      <Hero />
    </main>
  );
}
